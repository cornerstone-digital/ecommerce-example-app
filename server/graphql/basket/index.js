import basketTypes from './types';
import basketResolvers from './resolvers';

const types = {
  ...basketTypes
}

const resolvers = {
  ...basketResolvers
};

const mutations = {

};

export default {
  types,
  resolvers,
  mutations
}